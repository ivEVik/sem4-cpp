
#ifndef LAB_H
#define LAB_H


void bubbleSort(int*, const size_t, const bool);
void bubbleSortRange(int*, const size_t, const size_t, const bool);

void quickSort(int*, const size_t, const bool);
void quickSortRange(int*, const size_t, const size_t, const bool);

void printArray(const int*, const size_t);

void initArray(int*, const size_t);


#endif
